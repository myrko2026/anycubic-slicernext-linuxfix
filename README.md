# anycubic-slicernext-linuxfix
Unofficial Linux compatibility workaround for AnycubicSlicerNext. Fixes severe 3D viewport and print-bed stuttering on affected Linux systems, especially NVIDIA + Wayland, by automatically enabling a compatible Mesa Zink rendering path. Creates a standalone AppImage that can be launched directly.
